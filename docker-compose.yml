services:
  agent-vault:
    build: .
    image: agent-vault:local
    environment:
      S_VAULT_PATH: /data/vault.senv
      S_KEY: ${S_KEY:-change-me-for-local-testing}
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:8787:8787"
    command: web --host 0.0.0.0 --port 8787
